// Прокси-объект вызывает first()
proxy.first() {
    // Начинается транзакция
    // Вызывает target.first()
}

// Внутри целевого объекта:
public void first() {
    second();  // Внутренний вызов, минуя прокси!
}

protected void second() {
    // Выполняется в существующей транзакции
}

//Ответ: 1 транзакция